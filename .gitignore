.env
.env.*
!.env.example

node_modules/
.claude/
.grok/
.remember/
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/

# Anchored to the repo root deliberately -- an earlier un-anchored `data/`
# here matched (and gitignored) infra/azure/modules/data/ too, silently
# masking that whole Terraform module from git (#47).
/data/
uploads/
playwright-report/
test-results/
eval-report.json
ingestion-benchmark-report.json
search-benchmark-report.json
.DS_Store

graphify-out/

# MkDocs build output
site/
.memory/

# Terraform working dirs (any root/module) — state and providers never belong in git.
**/.terraform/
*.tfstate
*.tfstate.*
