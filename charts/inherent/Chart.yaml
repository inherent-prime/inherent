apiVersion: v2
name: inherent
description: Inherent application workloads (public-api, ingestion, weaviate, minio, temporal) for AKS — issue #323
type: application
version: 0.1.0
# appVersion tracks inherent_version (root tfvar); helm_release (modules/apps)
# overrides image.tag per-value, this is just the chart's own metadata default.
appVersion: "0.6.0"
keywords:
  - inherent
  - rag
  - azure
  - aks
